Follow the rules and verification checklist in AGENTS.md at repository root. Before finishing any implementation, run: backend tests (cd app && mvn verify), frontend tests and lint (cd ui && npm run test && npm run lint), recreate containers (docker compose down && docker compose up --build -d), and if Markdown was changed run npm run lint:docs. Do not commit or push unless explicitly asked.
